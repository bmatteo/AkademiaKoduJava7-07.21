public class ehfgksj {
    /*final int tabSize = 100;

        Random r = new Random();
        int looking = r.nextInt(10000) + 1;

        System.out.println("Szukana liczba: " + looking);

        int[][] tab = new int[tabSize][tabSize];

        System.out.println(tab.length);

        for(int i = 0; i < tab.length; i++) {
            for(int j = 0; j < tab.length; j++) {
                Random random = new Random();
                tab[i][j] = random.nextInt(10000) + 1;
                System.out.print(tab[i][j] + " ");
            }
            System.out.println();
        }

        boolean flag = false;

        for (int i = 0; i < 100; i++) {
            for (int j = 0; j < 100; j++) {
                if(tab[i][j] == looking) {
                    System.out.println("I: " + i + " J: " + j);
                    flag = true;
                    break;
                }
            }
        }

        if (!flag) {
            System.out.println("Nie znalezione");
        }*/
}
